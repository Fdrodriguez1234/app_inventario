<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Usuarios</title>
    <link rel="stylesheet" href="/css/estilos_usuarios.css"> <!-- Asegúrate de tener un CSS para estilos -->
</head>
<body>
    <div class="container">
        <h1>Gestión de Usuarios</h1>
        <a href="/agregar_usuarios"><button>Crear Usuario</button></a>
        
        <h2>Lista de Usuarios</h2>
        
        <% if (usuarios.length === 0) { %>
            <p>No hay usuarios en la base de datos. Debes crear un usuario.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Correo</th>
                        <th>Rol</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody id="usuariosList">
                    <% usuarios.forEach(usuario => { %>
                        <tr>
                            <td><%= usuario.id %></td>
                            <td><%= usuario.nombre %></td>
                            <td><%= usuario.correo %></td>
                            <td><%= usuario.rol %></td>
                            <td>
                                <a href="/editar_usuarios/<%= usuario.id %>"><button>Editar</button></a>
                                <a href=""><button>Eliminar</button></a>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
        
    </div>
</body>
</html>
