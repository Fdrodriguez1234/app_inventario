<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Categoría</title>
    <link rel="stylesheet" href="/css/estilos_agregar_categorias.css">
</head>
<body>
    <div class="container">
        <h1>Agregar Categoría</h1>

        <form action="/categorias/agregar_categorias" method="POST" class="form-categoria">
            <div class="form-group">
                <label for="nombre">Nombre de la Categoría:</label>
                <input type="text" id="nombre" name="nombre" placeholder="Ingresa el nombre de la categoría" required>
            </div>

            <div class="form-group">
                <label for="descripcion">Descripción:</label>
                <textarea id="descripcion" name="descripcion" rows="3" placeholder="Descripción de la categoría" required></textarea>
            </div>

            <div class="form-group">
                <label for="idPadre">Categoría Padre:</label> 
                <select id="idPadre" name="idPadre">
                    <option value="">Selecciona la categoría padre</option>
                    <% categorias.forEach(categoria => { %>
                        <option value="<%= categoria.id %>" <%= categoria.id === categoria.idPadre ? 'selected' : '' %>><%= categoria.nombre %></option>
                    <% }) %>
                </select>
            </div>

            <button type="submit" class="btn-agregar">Agregar Categoría</button>
            <button type="button" class="btn-cancelar" onclick="window.history.back();">Cancelar</button>
        </form>
    </div>
</body>
</html>